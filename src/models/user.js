export default class User {
  constructor(username, password, nisn, nama, kelas, alamat ) {
    this.username = username;
    this.password = password;
    this.nisn = nisn;
    this.nama = nama;
    this.kelas = kelas;
    this.alamat = alamat;
  }
}